# Implementation Package Registry

This directory is the active package registry for implementation tracking.

Primary records:

- `index.json` - canonical package/progress/capability index
- `PKG-*.md` - package-level records and closure reports
- `history/` - immutable historical snapshots for restart continuity

Current synchronized package state is recorded in:

- `PKG-4B.1-REPOSITORY-SNAPSHOT-PROGRESS-SYNCHRONIZATION.md`
